{
    module-id:  'io.liveoak.mongo',
    dependencies: ['mongo-launcher'],
    config: {
      db: "liveoak",
      servers: [
          {
            host: '${mongo.host}',
            port: ${mongo.port}
          }
      ]
    }
}
